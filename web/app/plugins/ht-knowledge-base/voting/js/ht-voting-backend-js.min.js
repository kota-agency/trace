jQuery(document).ready(function($){"use strict";$("button.ht-voting-delete-vote").each(function(index){var deleteButton=$(this),postID=deleteButton.attr("data-post-id"),voteID=deleteButton.attr("data-vote-id"),voteKey=deleteButton.attr("data-vote-key"),parentRow=deleteButton.parents("tr"),ajaxnonce=voting.ajaxnonce,challenge=voting.deleteChallenge;deleteButton.click(function(event){if(event.preventDefault(),!0===confirm(challenge)){var oldText=deleteButton.text();deleteButton.html('<img src="'+voting.spinner+'"/>'),parentRow.addClass("delete-row");var data={action:"ht_voting_delete_vote",key:voteKey,nonce:ajaxnonce,pid:postID,vid:voteID};$.post(voting.ajaxurl,data,function(response){"ok"==response?parentRow.animate({backgroundColor:"#969696"},2e3,function(){dataTable.row(".delete-row").remove().draw(!1)}):(parentRow.removeClass("delete-row"),deleteButton.text(oldText))})}})}),$(".ht-voting-article-voting-actions button").each(function(index){var actionButton=$(this),actionHref=actionButton.attr("href"),challenge=actionButton.attr("data-challenge");actionButton.click(function(event){event.preventDefault(),!0===confirm(challenge)&&(window.location.href=actionHref)})});var dataTable=$(".ht-voting-backend-vote-list").DataTable()});